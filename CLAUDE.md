# CLAUDE.md

Jekyll **Chirpy** 테마 기반 블로그. 글은 `_posts/` 아래 카테고리 디렉터리에 마크다운으로 작성한다.

## 이미지 처리

이미지는 **`/assets/img/`** 에 두고 글에서는 **루트 절대경로**로 참조한다.

```markdown
![설명 alt 텍스트](/assets/img/<post-slug>-img<N>.png)
```

- **저장 위치**: 모든 이미지는 `assets/img/` 에 모은다. (글과 같은 디렉터리에 두지 않는다)
- **파일명**: `<글-주제-slug>-img<번호>.png` 형식. 예) `prepared-statement-img1.png`, `aws-vpc-img2.png`
- **참조 경로**: 반드시 `/assets/img/...` (루트부터). `_config.yml`의 `img_cdn`(jsdelivr)이 이 경로 앞에 붙어 배포 시 CDN으로 서빙된다.
- **alt 텍스트**: 파일명(`img_2.png`) 말고 내용을 설명하는 문구를 쓴다.

### ⚠️ 상대경로 `![img.png](img.png)` 는 쓰지 말 것

에디터(IntelliJ 등)에서 글과 같은 폴더에 이미지를 붙여넣으면 `![img_2.png](img_2.png)` 같은 상대경로가 생기는데, 이건 **로컬 에디터 미리보기에서만 보이고 배포된 사이트에서는 깨진다.** 글 작성 중 이런 참조가 보이면:

1. 해당 이미지를 `assets/img/`로 복사하면서 의미 있는 이름으로 바꾼다
   ```bash
   cp "_posts/.../img_2.png" assets/img/<post-slug>-img1.png
   ```
2. 본문 참조를 `/assets/img/<post-slug>-img1.png`로 교체한다

## 마크다운 작성 규칙

- **표**: ASCII 박스(`┌─┬─┐`)가 아니라 마크다운 표(`| --- |`)로 작성한다. ASCII 박스는 본문에 그대로 두면 깨지므로, 다이어그램 용도라면 코드펜스(```)로 감싼다.
- **코드/다이어그램**: 호출 흐름도·패킷 구조 등 ASCII 아트는 반드시 코드펜스 안에 둔다. 펜스 밖에 두면 kramdown이 줄을 합쳐 깨진다.
- **한 줄 정리 / 콜아웃**: `>` blockquote를 쓴다. (`{: .prompt-info }` 같은 Chirpy 콜아웃 문법은 이 블로그에서 쓰지 않는다)
- **식별자**: 클래스/메서드/옵션명(`COM_QUERY`, `StringUtils.escapeString` 등)은 인라인 코드로 감싼다.
- **섹션 구분선**: `##`(h2) 헤딩 아래에는 `---`(수평선)을 넣어 화면에서 섹션이 시각적으로 구분되게 한다. 렌더링 시 hr로 나오도록 앞뒤에 빈 줄을 둔다 (`## 헤딩` → 빈 줄 → `---` → 빈 줄 → 본문). `###` 이하 하위 헤딩에는 넣지 않는다.
- **섹션 도입 문장**: `##`(h2) 섹션의 구분선(`---`) 다음에 오는 **첫 문장은 `>` blockquote로 감싼다.** 그 섹션이 무슨 얘기인지 한눈에 들어오게 하는 리드 문장 역할이다. (`###` 이하 하위 헤딩에는 적용하지 않는다)
- **군더더기·주관 배제**: 사실 전달과 무관한 도입 상투구("여기서 헷갈리기 쉬운 게 있다", "한 가지 짚고 갈 게 있다" 등)와 주관적 평가("~가 재밌다", "실무에서 아주 유용하다", "~할 때 딱이다", "꺼내는 카드다", "맥가이버칼" 같은 수사)는 쓰지 않는다. 바로 사실·설명으로 들어간다.
- **`<details>` 접기 블록**: 안에 마크다운(코드펜스 등)을 넣을 땐 반드시 `<details markdown="1">` 로 연다. 안 붙이면 kramdown이 내부를 raw HTML로 취급해 ```` ``` ```` 펜스가 코드블록으로 변환되지 않고, 코드 속 `<T>`·`CompletableFuture<String>` 같은 꺾쇠가 가짜 태그로 파싱돼 HTMLProofer(`tools/test.sh`) 빌드가 깨진다.
- **참고 자료 표기**: 글을 쓰거나 보강하며 공식 문서·스펙 등 외부 출처를 참고했다면, 해당 글 맨 아래 `## 참고 자료` 섹션에 링크를 추가한다. 없으면 섹션을 새로 만든다. 링크는 `[제목 (도메인/경로)](URL)` 형식으로 무슨 문서인지 알아볼 수 있게 쓴다.

## 내용 검증 (실제로 돌려보기)

글의 예시·동작 설명은 **머릿속 추론이나 "일반적으로 이렇다"는 통념에 기대지 말고, 가능하면 실제로 돌려서 확인한 결과를 쓴다.** 명령 출력, 쿼리 결과, 코드 실행 결과처럼 재현 가능한 것이면 특히 그렇다.

- **재현 가능하면 재현한다**: 로컬 도구, 도커 컨테이너, 간단한 스크립트 등으로 직접 실행해 결과를 확인한 뒤 그 값을 싣는다. (예: DB 락 동작이라면 도커로 DB를 띄워 락 상태를 직접 조회해 확인하는 식.)
- **버전을 명시한다**: 동작이 버전·환경에 따라 갈리는 주제는 검증에 쓴 버전·환경을 본문에 밝힌다. 옛 버전과 동작이 다르면 그 차이도 적는다.
- **통념을 그대로 옮기지 않는다**: 블로그·강의에서 자주 도는 규칙이라도 현재 버전에서 재현되는지 확인한다. (문서나 옛 버전 기준 설명이 지금 환경에서 다르게 동작할 수 있다.)
- **검증 못 하면 표시한다**: 사정상 못 돌려본 내용은 단정하지 말고, 추정임을 드러내거나 독자가 직접 확인하도록 안내한다.

## 글 front matter

```yaml
---
title: ...
date: YYYY-MM-DD HH:MM:SS +0900
categories: [상위, 하위]
tags: [Tag]
---
```

파일명은 `YYYY-MM-DD-slug.md` 형식.
