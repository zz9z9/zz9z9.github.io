---
title: Jenkins 살펴보기
date: 2025-12-22 22:10:00 +0900
categories: [지식 더하기, 이론]
tags: [Jenkins]
---

spring batch job 스케쥴러로써의 jenkins 자료는 많지 않은듯

## 버전
> 글 작성시점(25.12.22), LTS 버전 기준으로 2.528.3 패치 릴리즈가 25.12.10애 반영됨 (https://www.jenkins.io/changelog-stable/)

### LTS Release Line
> https://www.jenkins.io/download/lts/

- Weekly Jenkins 릴리즈는 빠르게 버그 수정과 새로운 기능을 사용자와 플러그인 개발자에게 전달한다.
- 하지만 보다 보수적인 사용자들에게는, 기능은 다소 뒤처지더라도 변경이 적고 중요한 버그 및 보안 수정만 받는 릴리즈 라인을 사용하는 것이 더 바람직하다.
- 여러 회사들은 안정화 및 내부 커스터마이징을 위해 Jenkins에서 파생된 자체 private 브랜치를 유지하고 있다.
- Jenkins 커뮤니티는 이러한 안정화 노력을 LTS 릴리즈 라인으로 옮기도록 권장한다.
- 이것을 Jenkins Long-Term Support(LTS) 릴리즈라고 부른다.

### LTS 릴리즈 모델
- 매 12주마다, 커뮤니티는 비교적 최근에 나온 릴리즈 중 하나를 합의(consensus)를 통해 선택하고, 이를 다음 “안정적이지만 오래된(stable but older)” 릴리즈 라인의 기준(baseline)으로 지정한다.
- 선택된 기준 버전을 X라고 가정하면, 버전 X에서 브랜치를 따서, X.1, X.2, X.3 같은 안정적인 패치 릴리즈를 만든다.
- 즉, X = 기준, X.1 / X.2 / X.3 = LTS 마이너 릴리즈
  - 새로운 기능 ❌
  - 안정화 패치만 ⭕

- 즉, 최소 1주 이상 메인 릴리즈에 포함되어 있었던 커밋만 대상이 된다.
- 하나의 baseline에 대해 4주 주기로 총 3개의 마이너 릴리즈가 나온다.
- 각 마이너 릴리즈 전에 2주 동안 RC(Release Candidate)를 배포한다.

핵심 흐름

| Week | 주요 이벤트 | 설명 |
|-----:|------------|------|
| 0    | X.3 Release | 이전 LTS baseline(X) 기준 마지막 마이너 릴리즈 배포 |
| 2    | X.1 RC     | X.1 LTS 릴리즈를 위한 Release Candidate 배포 (백포트 종료 후 테스트 시작) |
| 4    | X.1        | X.1 LTS 정식 릴리즈 |
| 6    | X.2 RC     | X.2 LTS 릴리즈를 위한 Release Candidate 배포 |
| 8    | X.2        | X.2 LTS 정식 릴리즈 |
| 10   | X.3 RC / Baseline 선택 | X.3 RC 배포 및 다음 LTS baseline(Y) 선정 |
| 12   | X.3        | X baseline 기준 최종 LTS 릴리즈 배포, 사이클 종료 |


이후 Y.1 사이클 시작

- Jenkins는 일반적으로 업그레이드는 가능하지만 다운그레이드는 어렵다.
  - Jenkins 내부 데이터 구조: forward-only
  - 플러그인 / job config / core 데이터 : 새 버전 기준으로 저장됨
  - 따라서, downgrade = 데이터 불일치 위험

## 설치 방법
> 관심 있는 것만 살펴보았다. (https://www.jenkins.io/doc/book/installing/)

### 1. With Docker
> Docker는 애플리케이션을 컨테이너(container) 라는 격리된 환경에서 실행하기 위한 플랫폼이다.

- Jenkins 같은 애플리케이션은 읽기 전용 이미지(image) 형태로 다운로드할 수 있다.
- Docker 이미지는 영구적으로 저장되며 새 버전이 나올 때만 갱신되지만, 컨테이너는 본질적으로 일시적이다.
  - 그래서 Jenkins 같은 stateful 서비스는: 데이터 볼륨 분리 필수

- Docker의 기본 설계 덕분에 Jenkins 같은 Docker 이미지는 Docker만 실행 가능하다면 어떤 OS나 클라우드에서도 동일하게 실행할 수 있다.
- Linux에서 Docker를 설치할 경우, root 권한 없이 Docker를 관리할 수 있도록 설정해야 한다.
  - root로만 Docker 사용
  - CI 서버(Jenkins)는 보통 일반 사용자로 실행
  - 그래서:
    - docker 그룹 설정 필요
    - 부팅 시 자동 시작 설정 필요

**Jenkins 이미지 선택**
- Jenkins용 Docker 이미지는 여러 개가 있다.
- 공식적으로 권장되는 이미지는 `jenkins/jenkins`이다.
- 이 이미지는 현재 Jenkins LTS 버전을 포함하며, 운영 환경에서 사용 가능하다.

**Jenkins 이미지 커스터마이징**

- 도커 파일 예시

```
# Jenkins 공식 LTS 이미지 사용
# - 2.528.3 : Jenkins LTS 버전
# - jdk21   : Jenkins 실행에 필요한 JDK 21 포함
#   (컨테이너 안에서 별도 Java 설치 불필요)
FROM jenkins/jenkins:2.528.3-jdk21


# 기본 jenkins/jenkins 이미지는 보안상 jenkins 사용자로 실행됨
# 패키지 설치(apt-get)는 root 권한이 필요하므로 root로 전환
USER root


# lsb-release 패키지 설치
# - 현재 OS의 배포판 코드네임(bullseye, bookworm 등)을 알아내기 위해 필요
# - 이후 Docker apt repository 설정에서 $(lsb_release -cs)에 사용됨
RUN apt-get update && apt-get install -y lsb-release


# Docker 공식 GPG 키 다운로드
# - Docker 패키지를 신뢰할 수 있는 출처로 검증하기 위한 키
# - /usr/share/keyrings 하위에 저장하는 것은 최신 Debian 권장 방식
RUN curl -fsSLo /usr/share/keyrings/docker-archive-keyring.asc \
  https://download.docker.com/linux/debian/gpg


# Docker apt repository 등록
# - 현재 아키텍처(amd64, arm64 등)를 자동으로 감지
# - signed-by 옵션으로 위에서 받은 GPG 키를 사용하도록 지정
# - Debian 기반 Jenkins 이미지(OS)에 맞는 Docker 저장소 추가
RUN echo "deb [arch=$(dpkg --print-architecture) \
  signed-by=/usr/share/keyrings/docker-archive-keyring.asc] \
  https://download.docker.com/linux/debian \
  $(lsb_release -cs) stable" > /etc/apt/sources.list.d/docker.list


# Docker CLI 설치
# - docker-ce-cli만 설치 (Docker 데몬은 설치하지 않음)
# - 실제 Docker 데몬은 docker:dind 컨테이너에서 실행됨
# - Jenkins 컨테이너는 CLI만 사용해서 원격 Docker 데몬에 명령 전달
RUN apt-get update && apt-get install -y docker-ce-cli


# 보안상 root 상태로 Jenkins를 실행하는 것은 권장되지 않음
# 다시 jenkins 사용자로 전환
USER jenkins


# Jenkins 플러그인 설치
# - blueocean        : 시각적인 Pipeline UI
# - docker-workflow  : Jenkins Pipeline에서 docker 관련 DSL 사용
# - json-path-api    : 여러 플러그인에서 의존하는 공통 라이브러리
#
# jenkins-plugin-cli를 사용하면:
# - 컨테이너 빌드 시점에 플러그인이 고정됨
# - 컨테이너 실행 후 플러그인 설치로 인한 변동성 감소
RUN jenkins-plugin-cli --plugins "blueocean docker-workflow json-path-api"
```

- 빌드 : `docker build -t myjenkins-blueocean:2.528.3-1`

Jenkins 컨테이너 실행

이 컨테이너가 실제 Jenkins Controller야.

핵심 환경 변수
DOCKER_HOST=tcp://docker:2376
DOCKER_TLS_VERIFY=1


➡️ Jenkins가:

docker:dind 컨테이너의 Docker 데몬에

TLS로 안전하게 연결

포트 매핑

8080

Jenkins Web UI

50000

Inbound Jenkins Agent (TCP)

📌 WebSocket Agent만 쓰면 50000 필요 없음

볼륨 매핑

/var/jenkins_home

Jenkins 설정 / job / workspace

/certs/client

Docker TLS 인증서


### 2. With War File
> Jenkins Web Application Archive(WAR) 파일은 Jetty 서블릿 컨테이너를 감싸는(wrapper) Winstone을 함께 포함하고 있으며,
Jenkins가 지원하는 Java 버전만 있다면 어떤 운영체제나 플랫폼에서도 실행할 수 있다.

- 일반적인 WAR 파일
  - Tomcat / Jetty 같은 WAS에 배포해야 함

- Jenkins WAR 파일
  - 자체적으로 Jetty(Winstone 포함)를 내장
  - `java -jar jenkins.war` 만으로 서버처럼 바로 실행됨
  - 즉, `Jenkins WAR = Jenkins + Embedded Servlet Container` (Jetty/Winstone)
  - 따라서, Tomcat 설치 등 필요없이 Java만 있으면 바로 실행 가능 (Spring Boot의 `java -jar app.jar` 실행 방식과 매우 비슷한 개념)

- WAR 방식 Jenkins는:
  - 가장 단순한 설치 방식
  - Java만 있으면 됨

- 특징:
  - Embedded Jetty 포함
  - OS 독립적
  - 개발/테스트/PoC에 매우 적합

- 단점:
  - `서비스 관리`, `자동 재시작`, `로그/보안 설정`을 직접 챙겨야 함

## 그래서 Jenkins 같은 stateful 서비스는: * **데이터 볼륨 분리 필수** 이런 특성때문에, 젠킨스를 컨테이너로 배포하지 않는다는 얘기도 들었던것 같은데 넌 어떻게 생각해 ?

## Java 버전 호환성
> https://www.jenkins.io/doc/book/platform-information/support-policy-java/

| Supported Java versions            | Long term support (LTS) release | Weekly release              |
|-----------------------------------|----------------------------------|-----------------------------|
| Java 17, Java 21, or Java 25       | N/A                              | 2.534 (October 2025)        |
| Java 17 or Java 21                 | 2.479.1 (October 2024)           | 2.463 (June 2024)           |
| Java 11, Java 17, or Java 21       | 2.426.1 (November 2023)          | 2.419 (August 2023)         |
| Java 11 or Java 17                 | 2.361.1 (September 2022)         | 2.357 (June 2022)           |
| Java 8, Java 11, or Java 17        | 2.346.1 (June 2022)              | 2.340 (March 2022)          |
| Java 8 or Java 11                  | 2.164.1 (March 2019)             | 2.164 (February 2019)       |
| Java 8                             | 2.60.1 (June 2017)               | 2.54 (April 2017)           |
| Java 7                             | 1.625.1 (October 2015)           | 1.612 (May 2015)            |


## Pipeline

## Scaling

## 실무
- https://techblog.woowahan.com/2722/
- https://jojoldu.tistory.com/489

## 참고 자료
---

- https://www.jenkins.io/doc/book/
